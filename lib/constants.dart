import 'package:flutter/material.dart';

// app color usage setup

const appSecondaryColor = Color(0xFFFE6D8E);
const appTextColor = Color(0xFF12153D);
const appTextLightColor = Color(0xFF9A9BB2);
const appFillStarColor = Color(0xFFFCC419);

// padding and margin details
const appDefaultPadding = 20.0;

const appDefaultShadow =
    BoxShadow(offset: Offset(0, 4), blurRadius: 4, color: Colors.black26);
